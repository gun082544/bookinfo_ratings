name: dev-env
on:
  push:
    branches:
      - dev
jobs:
  deploy-dev:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - run: docker build -t ghcr.io/gun082544/bookinfo_ratings:dev .
      - run: docker images
      - run: docker push ghcr.io/gun082544/bookinfo_ratings:dev